import 'package:flutter/material.dart';

ThemeData appData() {
  return ThemeData(
    primarySwatch: Colors.blue,
    textTheme: TextTheme(
      headline4: TextStyle(
        color: Color(0xFF425398)
      )
    )
  );
}